name: bc_taxi
description: BC Taxi - apps passageiro e motorista
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.4.0
  web_socket_channel: ^3.0.3
  shared_preferences: ^2.5.3
  geolocator: ^13.0.4
  provider: ^6.1.5
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  qr_flutter: ^4.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
